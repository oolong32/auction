{% extends "layout.nunjucks" %}

{% block content %}
  <h2>{{ title }}</h2>
  <dl>
    <dt>Betrag</dt>
    <dd>CHF {{ bid.amount }}</dd>
    <dt>Bild</dt>
    <dd><a href="{{ bid.article.url }}">{{ bid.article.title }}</a></dd>
    <dt>Bieter</dt>
    <dd><a href="{{ bid.user.url }}">{{ bid.user.name }}</a></dd>
  </dl>
  <hr>
  <p><a href="{{ bid.url }}/delete">Gebot löschen</a></p>
{% endblock %}

